import { FeatureEmptyState } from '@/components/feature-empty-state';

export default function ReadTab() {
  return <FeatureEmptyState symbol="▤" title="Your stories live here" zh="你的故事都在這裡" body="Assigned stories and reading passages will appear here when they are ready." />;
}
